# v0.3 migration friction log

Every hand-correction, skeleton gap, and judgment the references failed to supply, recorded per dogfood gate. Recurring items are structural and get fixed in checklist item 10; one-offs are notes.

## Gate 1 — `vibe-plugins/CLAUDE.md` (147 → 46 lines)

**Skeleton performance: clean.** No structural correction needed. The seven sections absorbed all surviving content without forcing a shape that did not fit, and the two-axis gate resolved every line without a coin flip. Item 4's stop condition did not trigger.

### F-01 — Axis 2 carried real weight on its first live run — CONFIRMS DESIGN

Not a defect. Recording it because it is the first evidence outside the worked examples.

Three lines in this file were derivable and would have been cut by the single-axis gate:

- "The load-bearing artifact is `.claude-plugin/marketplace.json`" — inferable by reading the repo; the default assumption (README or `package.json` is the center) is wrong.
- "No formatter is enforced — match the existing file's style" — inferable from the absence of a formatter config; the default assumption (reach for prettier) is wrong.
- "Tag naming diverges for `vibe-test` and `vibe-sec`" — `git tag` shows it; the divergence reads like a mistake to fix.

All three survived on axis 2. The single-axis version specced in the original spec-bank document would have lost all three.

### F-02 — Guard prong 3 fired correctly on its first live run — CONFIRMS DESIGN

The voice block duplicated the global file's 626Labs voice rules. Prong 3 required establishing a canonical home before treating it as duplicated: the global file is canonical, this repo is not, so dedup was safe. Result is a one-line pointer plus the three genuinely repo-specific facts (storefront framing, tagline, no-emoji-in-marketing).

Had this been the repo that *holds* the canonical voice guide, prong 3 would have blocked the dedup. The prong is doing the job it was written for.

### F-03 — Estate-wide stale voice rule — NOT A SKELETON DEFECT, but a real find

**Severity: high. Scope: 12+ repos. Owner: Este.**

`vibe-plugins/CLAUDE.md` said "Em-dashes welcome." The global `~/.claude-personal/CLAUDE.md` says "Em-dashes minimal. Commas, periods, colons by default," and states explicitly that *"the earlier 'em-dashes welcome' framing here was an artifact of a misread 'I don't care about em-dashes' remark; corrected 2026-05-26."*

The correction landed in the global file and never propagated to repo keystones. A sweep found the stale rule live in:

| Repo | Line |
|---|---|
| `626-tenets-thesis` | 81 |
| `626Labs-Publishing/works/626-tenets-thesis` | 81 |
| `626Labs-Publishing/works/2026-05-26-misalignment-incoherence` | 79 |
| `626Labs-Publishing/works/2026-05-26-coscientist-novelty` | 79 |
| `626Labs-Publishing/works/2026-05-26-containment` | 119 |
| `626-labs-mod-manager` | 22 |
| `626labs-hub` | 26 |
| `vibe-doc` | 10 |
| `vibe-cartographer` | 10 |
| `vibe-thesis` | 17 |
| `ROROROblox` | 11 |
| `_old-app-readinessplugin` | 10 (archive) |

Only the `dotclaude` mirrors carry the corrected rule, because they mirror the global file rather than being repo keystones.

**Why this matters beyond the typo:** every one of those repos hands an agent a voice rule that contradicts the global one loaded in the same session. That is precisely the conflicting-instruction failure the Claude 5 context-engineering guidance names — *"several conflicting messages in a single request"* — running live across a dozen repos. The agent has to spend judgment resolving a conflict that should not exist.

**Three of the twelve are inside this cycle's migration set** (`vibe-plugins` fixed, `vibe-cartographer` at gate 3). The other nine are outside scope and need a separate sweep. Este flagged the systemic nature himself at gate 1: *"that is probably the same in every repo."*

**Root cause worth naming:** voice rules were copied into repo keystones rather than pointed at. Guard prong 3 exists to prevent exactly this going forward — dedup points at the canonical copy instead of duplicating it — but it cannot retroactively fix files written before v0.3.

### F-04 — Decision-log MCP namespace inconsistency — DEFERRED, Este-owned

The file referenced `mcp__626Labs__manage_decisions`; the global references `mcp__626labs-cloud__manage_decisions`. Both namespaces exist in this session's tool list, so this could not be resolved from inside the migration.

Sidestepped rather than guessed: the section was rewritten to the family's backend-agnostic decision-log convention (auto-detect when present, file or tracker as fallback, "none" first-class), which is correct regardless of which namespace is canonical. Este confirmed at gate 1 that he believes both work and will verify separately.

### Cuts that needed no judgment

The 15-row plugin roster table was the largest single deletion (17 lines) and the easiest call — the file already contained both the rule that the manifest is the source of truth and the one-liner that regenerates the table. It was documentation of a thing that documents itself.

"Don't commit secrets" was cut as exactly the filler a 3-item floor manufactures. Removing the floor removed the incentive to write it.

## Gate 2 — `Project-626Labs-1/CLAUDE.md` (302 lines) — BLOCKED, and it found the cycle's worst defect

**Skeleton performance: a real defect, fixed at the gate.** The migration did not proceed. Reading the file surfaced a failure mode neither v0.2.1 nor the v0.3 spec accounted for.

### F-05 — Keystone would overwrite generated files — SHIPPED-DEFECT CLASS, fixed in `f985e5c`

**Severity: critical. This would have shipped.**

`Project-626Labs-1/CLAUDE.md` is not hand-authored. It has two owners:

| Lines | Owner | Regenerated by |
|---|---|---|
| 1-201 | `CLAUDE.template.md` (191 lines, `{{agents.lead.name}}` interpolation) | `npm run vars:resolve` → `scripts/vars-resolver.ts` (34KB) + `agent-vars.json` + a JSON schema |
| 202-302 | GitNexus tooling, `<!-- gitnexus:start -->` / `<!-- gitnexus:end -->`, absent from the template | `npx gitnexus analyze`, plus a PostToolUse hook after every commit |

Nothing in Keystone asks whether the file it is about to regenerate is itself generated. Run `/keystone` here and it produces a well-formed CLAUDE.md that is **silently erased on the next `npm run vars:resolve`**, with the GitNexus block re-injected after the next commit. No error, no warning, no signal that the work evaporated.

The v0.2.1 skill's only guard is "read it before overwriting; show a diff." A diff against a generated file looks completely normal.

**Fix applied at the gate rather than deferred:** `skeleton.md` gains a "Before you write: find out what owns the file" section — detect generated-file headers, sibling templates, interpolation syntax, render scripts in `package.json`/Makefile, and tool-owned marker regions; retarget the template; leave tool-owned regions alone; tell the builder before writing. A file can have more than one owner, so the ownership map is established before the first edit.

**Why this is the cycle's most valuable find:** it is invisible to every check that existed. It is not a style question or a judgment call. It is a class of repo where the tool's core operation silently does nothing, and only a real-file dogfood on a repo with real tooling could surface it. Gate 1 on a hand-authored file could never have found it.

**Provenance note:** the checklist anticipated this class of trap — item 9 tells the migration to check `vibe-cartographer` for a generator before cutting, because the harness comparison had flagged it as "half auto-generated." The trap was real; it was just in a different repo, three items earlier than expected.

### F-06 — The spec's surface guess was wrong — CONFIRMS THE VERIFY-FIRST INSTRUCTION

The spec assumed three surfaces for this repo (web, VS Code extension, MCP server). Verification against the actual tree found **seven**: root web app, `functions/`, `mcp-server/`, `tools/vscode-extension/`, `shared/types/`, `docker/`, `plugins/roblox-skin-creator/`.

Also found a trap for nested-keystone proposals: `.worktrees/` contains three `CLAUDE.md` files. Those are **git worktrees, not surfaces**. Proposing nested keystones there would be wrong, and a naive "find every directory with a CLAUDE.md" heuristic would have done exactly that.

`progressive-disclosure.md` already says to verify surfaces against the actual tree rather than infer them from the repo's name. That instruction earned its place on first contact. **Worth adding `.worktrees/` and other VCS-internal directories to the detection guidance as an explicit exclusion** — deferred to item 10.

### F-07 — Persona content at scale is an unresolved question — NEEDS ESTE

Lines 12-109 are ~98 lines of the "Kira" persona: identity, how-you-think, how-you-operate, communication style, what-you-never-do. Under guard prong 1 this is protected content, and under prong 3 this repo is its canonical home, so none of it can be cut or deduped away.

But roughly a third of it duplicates the global file's operating process nearly verbatim — "Gather context. Git log. You never start blind," blast-radius assessment, right-process-for-the-task. That part is genuinely duplicated against `~/.claude/CLAUDE.md` and prong 3 would allow pointing at it.

The unresolved part: the guard says protected content routes by frequency of need, and a persona is by definition needed on every task, so it stays inline. That yields a correct answer of "98 lines of persona stays." No rule in v0.3 says otherwise, and it may well be right. But it means a keystone can be budget-compliant on the derivable axis while still being very long, and the references currently do not discuss persona-at-scale at all.

Not fixed. Flagged for Este, because compressing someone's persona is a taste call, not a mechanical one.

### F-08 — `skeleton.md` now exceeds its own stated ceiling — MINOR, deferred to item 10

Adding the ownership section took `skeleton.md` from 88 to 108 lines, past the "under 100" acceptance criterion in checklist item 3.

Recording rather than silently moving the target. The 100 was an authoring estimate, and the produced-keystone budget does not govern Keystone's own reference files. But the ownership check is arguably a distinct concern (write-safety, not skeleton shape) and could be its own reference. Item 10 decides: accept the overage, or split.
